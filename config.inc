<?php

//path to application e.g /NF/FriendlyScore/
define('APP_PATH', '/NF/FriendlyScore/');

define('APPLICATION_NAME', 'FriendlyScore PHP task');

//path to client credential which has been generated by gmail api
define('CLIENT_SECRET_PATH', __DIR__ . '/client_secret.json');

define('SCOPES', implode(' ', array(
        Google_Service_Gmail::GMAIL_READONLY)
));

// Default user's account.
define('USER','me'); 

//Max result of returned emails
$MAX_RESULT = array('maxResults' => 100);

/**
 * for SAPI CLI
 */
//if (php_sapi_name() != 'cli') {
//    throw new Exception('This application must be run on the command line.');
//}